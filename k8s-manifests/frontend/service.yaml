apiVersion: v1
kind: Service
metadata:
  name: church-frontend
  namespace: church-app
  labels:
    app.kubernetes.io/name: church-frontend
    app.kubernetes.io/component: ui
    app.kubernetes.io/part-of: sunday-school-system
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: church-frontend
